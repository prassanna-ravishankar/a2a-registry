[project]
name = "hello-world-agent"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk[http-server]==1.0.0a3",
    "grpcio>=1.80.0",
    "grpcio-status>=1.80.0",
    "uvicorn>=0.34",
]
